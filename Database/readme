This directory stores the FFNN database and TOPOPT neural network (MATLAB .mat files).

For CNN database, please download the repo release as the database cannot be uploaded due to limit on the file size.

These database are include within each code as well as collected here.
